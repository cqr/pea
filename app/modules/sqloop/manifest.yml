name: Sequeloop
description: A simple, tiny ORM for PHP and MySQL which is tightly integrated with pea.
version: 0.5
require:
    -sqloopbase.class.php
    -sqloop.php
depends: mysqlconnector
autoloader: SqloopBase->autoload($class);
